import classes from "./Header.module.scss";

const Header = () => {
  return (
    <div className={classes.header}>
      <h1 className={classes.heading}>GOOGLE BOOKS CATALOGUE</h1>
    </div>
  );
};

export default Header;
